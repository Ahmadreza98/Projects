# Switch Case
The `switch-case` is an alternative statement to using multiple `if` or `else if` statements. it's actually more efficient when working with fixed integer values, and it also helps with readability. The structure is as follows:
```C
switch (expression)

    {

    case constant expression:

        /* code */

        break;

    default:

        break;

    }
```